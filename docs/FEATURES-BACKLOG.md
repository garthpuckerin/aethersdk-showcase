# Features backlog — aethersdk-showcase

> Routing rule: this file holds **capabilities that do not exist yet and may
> be built** (Work Item Routing Standard). Defects and deferred debt live in
> `ISSUES.md`, never here. Work moves to `ROADMAP.md` once it is decided and
> sequenced.

- **Second-source people flow.** Show Xperience as an alternate people system
  of record with the same canonical employee shape. The fixture already
  carries the connector; a seeded run and copy are missing.
- **Completion-to-Tableau drill-down.** A dedicated analytics surface for the
  learning-in flow instead of the current audit → delivery trail.
- **Exportable audit bundle.** The Auditor persona downloads a mock evidence
  bundle for one identity chain, mirroring the engine's evidence export.
- **Guided tour overlay.** Step-through highlights for the two signature
  workflows, replacing the text-only "Trace one seam" onboarding step.
